// *** BASIC COMMANDS ***

// -nmp start // Inicialize the server
// -npm build// To compile on the production stage

// -install -g create-react-app // Install the app globaly
// -create-react-app + Project's name // To create a new project

// *** MAIN LIBRARIES ***

// -REACT creates and feeds components with hooks to generate interactivity. It's also in charge of the communication between those components.

// -REACT DOM connects those components to the page. Uses RENDER() -method- to link componets to the DOM.
// -------REACTDOM Methods()----------
//  reactDOM.findDomNode()=> to find an element 
//  reactDOM.renderToString ()=> to convert a component or the whole app to a string. Most used for apps renderized on the server side.

//  -PROPTYPES to check data that enterS inside the components

// -------------------------JSX---------------------------------
// JavaScript+HTML=> allows to create DECLARATIVE COMPONENTS. What is returned is what is rendered or showed on the browser.

// var title = <h1> Hi World </h1> ___stands for at BABEL__ var title= React.createElement( "h1", null, "Hi World")

// reactDOM.render (app, root) APP goes for WHAT and root WHERE

// ELEMENT vs COMPONENT

// A component can be used many times and be overwritable. Must iniciate in uppercase, and also, return a node.


//A FUNCTIONAL COMPONENT is  FUNCTION THAT RETURNS AN ELEMENT.










